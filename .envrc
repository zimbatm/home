#!/usr/bin/env bash

# Flake devShell: wrapped `agenix` (edits secrets/*.age) + nixfmt.
# nix-direnv caches the build; it re-evaluates when flake.{nix,lock} change.
use flake

source_env_if_exists .envrc.local

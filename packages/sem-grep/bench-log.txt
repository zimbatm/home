# Falsification bench for `sem-grep log` — 10 real queries pulled from
# ops-deploy-nv1.md debugging history. Run each via `sem-grep log "<q>"` and
# compare against `journalctl -g`. Pass: ≥7/10 surface the relevant line in
# top-5. Fail → bge-small doesn't generalise to machine log text; kill this
# verb AND the live-caption→sem-grep nightly fold (caption jsonl is log-shaped).
wake-listen crash
wifi drop yesterday
openvino conversion error
NPU device not found
pipewire audio glitch
silero VAD type error
home-manager activation failed
GDM session refused login
restic backup failed
firefox native messaging disconnected

# Falsification bench for `sem-grep sig` (gated on ops-deploy-nv1):
# 1. re-run the existing 20q grind bench against `sig` vs body-chunk on the
#    "where is the function that…" subset; pass = sig recall ≥ body → flip
#    default to sig-first-then-body. Fail → bge-small can't do interface-shape;
#    look at jina-code/unixcoder next.
# 2. re-run packages/ask-local/bench-agent.jsonl with sem-grep-sig in
#    tools.json; pass = 3.8B picks sig for code-nav prompts AND token spend
#    drops vs sem-grep alone.
